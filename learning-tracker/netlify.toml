[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback: every route serves index.html so React Router handles it
# (prevents 404 on refresh of /dsa, /plan, /topics/..., etc.)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
